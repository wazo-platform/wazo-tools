---
services:
    postgres:
        image: postgres:15-bookworm
        environment:
            POSTGRES_PASSWORD: check-db-schema
        ports:
            - 127.0.0.1::5432
        volumes:
            - type: tmpfs
              target: /var/lib/postgresql/data
