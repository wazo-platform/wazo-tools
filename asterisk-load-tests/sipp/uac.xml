<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!-- UAC scenario: sends INVITE to Asterisk to drive the inbound call path -->
<scenario name="Load test UAC">

  <!-- Send INVITE -->
  <send retrans="500">
    <![CDATA[INVITE sip:loadtest@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: "SIPp UAC" <sip:sipp-uac@[local_ip]:[local_port]>;tag=[call_number]
To: <sip:loadtest@[remote_ip]:[remote_port]>
Call-ID: [call_id]
CSeq: 1 INVITE
Contact: <sip:sipp-uac@[local_ip]:[local_port];transport=[transport]>
Max-Forwards: 70
Content-Type: application/sdp
Content-Length: [len]

v=0
o=sipp 1 1 IN IP[local_ip_type] [local_ip]
s=sipp call
c=IN IP[media_ip_type] [media_ip]
t=0 0
m=audio [media_port] RTP/AVP 0 8
a=rtpmap:0 PCMU/8000
a=rtpmap:8 PCMA/8000

]]>
  </send>

  <!-- Expect 100 Trying -->
  <recv response="100" optional="true" timeout="5000">
  </recv>

  <!-- Expect 180 Ringing (optional, Asterisk may skip to 200) -->
  <recv response="180" optional="true" timeout="10000">
  </recv>

  <!-- Expect 183 Session Progress (optional) -->
  <recv response="183" optional="true" timeout="10000">
  </recv>

  <!-- Wait for 200 OK -->
  <recv response="200" timeout="30000" crlf="true">
  </recv>

  <!-- Send ACK -->
  <send>
    <![CDATA[ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: "SIPp UAC" <sip:sipp-uac@[local_ip]:[local_port]>;tag=[call_number]
To: <sip:loadtest@[remote_ip]:[remote_port]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 1 ACK
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- Hold for 5 seconds (call duration) -->
  <pause milliseconds="5000"/>

  <!-- Send BYE -->
  <send retrans="500">
    <![CDATA[BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
From: "SIPp UAC" <sip:sipp-uac@[local_ip]:[local_port]>;tag=[call_number]
To: <sip:loadtest@[remote_ip]:[remote_port]>[peer_tag_param]
Call-ID: [call_id]
CSeq: 2 BYE
Max-Forwards: 70
Content-Length: 0

]]>
  </send>

  <!-- Wait for 200 OK to BYE -->
  <recv response="200" timeout="5000" crlf="true">
  </recv>

</scenario>
