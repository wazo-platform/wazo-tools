.PHONY: clean

# Remove generated test output: logs, result files (including the versioned
# backups created by the runners) and captured core dumps.
clean:
	rm -f results/*.log results/*.json
	rm -f cores/core.* cores/*.core
